{% extends 'base.html.twig' %}
{% block title %}
    {{ parent() }} - All categories
{% endblock %}
{% block body %}
    <h1 class="bi-text-left pt-5 pb-5">Toutes les catégories de la table category :</h1>
    {% for category in categories %}
        <div class="card" style="width: 18rem;">
            <ul class="list-group list-group-flush">
                <li class="list-group-item">{{ loop.index }} - Catégorie : <a href="{{ path("category_show", {categoryName: category.name}) }}">{{ category.name }}</a></li>
            </ul>
        </div>
    {% else %}
    Aucune catégorie trouvée
    {% endfor %}
    <a href="{{ path('category_new') }}">Ajouter une catégorie</a>
    <br>
    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}
