<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <img src="{{ asset('build/logo.png') }}" alt="logo wild séries" width="100px"/>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link" href="{{ path('app_index') }}">Accueil</a>
            <a class="nav-item nav-link" href="{{ path('program_index') }}">Séries</a>
            <a class="nav-item nav-link" href="{{ path('category_index') }}">Catégories</a>
            <a class="nav-item nav-link" href="{{ path('actor_index') }}">Casting</a>
            <a class="nav-item nav-link" href="#">Mes favoris</a>
            {% if app.user %}
                {{ app.user.email }}
                <a class="nav-item nav-link" href="{{ path('app_logout') }}"> Se déconnecter</a>
            {% else %}
                <a class="nav-item nav-link" href="{{ path('app_login') }}"> Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>
